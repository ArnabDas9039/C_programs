#include<stdio.h>
#include<stdlib.h>
int mmain(){
    int x=rand(), y=rand();
    int w=-2*x-y, z=x+2*y;
    printf("The random coordinate is: (%d, %d, %d, %d)\n",x,y,z,w);
    int j=y; 
    int i=x-j;
}